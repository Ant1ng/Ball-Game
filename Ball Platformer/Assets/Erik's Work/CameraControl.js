#pragma strict

var player : GameObject;

var camera : GameObject;

function Start () {

//Use Unity to postion the camera the distance you want it to stay from the ball. This script will keep it that distance.

}

function Update () {

	if(Input.GetAxisRaw("Horizontal" < 0)){

	}

}